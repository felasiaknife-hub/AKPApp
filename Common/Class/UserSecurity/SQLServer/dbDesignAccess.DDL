--    This SQL DDL script was generated by Microsoft Visual Studio (Release Date: LOCAL BUILD).

--    Driver Used : Microsoft Visual Studio - Microsoft Access Driver.
--    Document    : D:\Krisana\Projects\Common\UserSecurity\dbDesign.vsd.
--    Time Created: 21 July 2003 1:30.
--    Operation   : From Visio Generate Wizard.
--    Connected data source : MS Access Database
--    Connected server      : ACCESS
--    Connected database    : D:\Krisana\Projects\Common\UserSecurity\ModSecurity.mdb



-- Create new table `tbFormList`.
-- `tbFormList` : Table of tbFormList
-- 	`FormName` : FormName identifies tbFormList
-- 	`FormDesc` : FormDesc is of tbFormList  
create table `tbFormList` ( 
	`FormName` TEXT(30),
	`FormDesc` TEXT(60), constraint `tbFormList_PK` primary key (`FormName`) ); 

-- Note: Access does not support the mandatory (not null) constraint in SQL DDL.
-- The mandatory constraints for table '`tbFormList`' will be created in the database using DAO
-- only if there is a connection to the Access MDB file during DDL generation.
-- Not nullable columns for table 'tbFormList' :
-- 	`FormName`


-- Create new table `tbAccessRole`.
-- `tbAccessRole` : Table of tbAccessRole
-- 	`RoleAccessID` : RoleAccessID identifies tbAccessRole
-- 	`GroupID` : GroupID is of tbAccessRole
-- 	`FormName` : FormName is of tbAccessRole
-- 	`CanAccess` : CanAccess is of tbAccessRole
-- 	`CanAdd` : CanAdd is of tbAccessRole
-- 	`CanUpdate` : CanUpdate is of tbAccessRole
-- 	`CanDelete` : CanDelete is of tbAccessRole
-- 	`CanPrint` : CanPrint is of tbAccessRole  
create table `tbAccessRole` ( 
	`RoleAccessID` TEXT(18),
	`GroupID` TEXT(18),
	`FormName` TEXT(30),
	`CanAccess` CHAR(1),
	`CanAdd` CHAR(1),
	`CanUpdate` CHAR(1),
	`CanDelete` CHAR(1),
	`CanPrint` CHAR(1), constraint `tbAccessRole_PK` primary key (`RoleAccessID`) ); 

-- Note: Access does not support the mandatory (not null) constraint in SQL DDL.
-- The mandatory constraints for table '`tbAccessRole`' will be created in the database using DAO
-- only if there is a connection to the Access MDB file during DDL generation.
-- Not nullable columns for table 'tbAccessRole' :
-- 	`RoleAccessID`


-- Note: Access does not support default values in SQL DDL.  Default values will be created in
-- the database using DAO only if there is a connection to the Access MDB file during DDL generation.
-- Columns with default values for table 'tbAccessRole' :
-- 	`CanAccess`
-- 	`CanAdd`
-- 	`CanUpdate`
-- 	`CanDelete`
-- 	`CanPrint`


-- Create new table `tbUser`.
-- `tbUser` : Table of tbUser
-- 	`UserID` : UserID identifies tbUser  
create table `tbUser` ( 
	`UserID` TEXT(18), constraint `tbUser_PK` primary key (`UserID`) ); 

-- Note: Access does not support the mandatory (not null) constraint in SQL DDL.
-- The mandatory constraints for table '`tbUser`' will be created in the database using DAO
-- only if there is a connection to the Access MDB file during DDL generation.
-- Not nullable columns for table 'tbUser' :
-- 	`UserID`


-- Create new table `tbUserInGroup`.
-- `tbUserInGroup` : Table of tbUserInGroup
-- 	`UserID` : UserID partly identifies tbUserInGroup
-- 	`GroupID` : GroupID identifies tbUserInGroup
-- 	`JoinDate` : JoinDate is of tbUserInGroup  
create table `tbUserInGroup` ( 
	`UserID` TEXT(18),
	`GroupID` TEXT(18),
	`JoinDate` TEXT(10), constraint `tbUserInGroup_PK` primary key (`GroupID`, `UserID`) ); 

-- Note: Access does not support the mandatory (not null) constraint in SQL DDL.
-- The mandatory constraints for table '`tbUserInGroup`' will be created in the database using DAO
-- only if there is a connection to the Access MDB file during DDL generation.
-- Not nullable columns for table 'tbUserInGroup' :
-- 	`UserID`
-- 	`GroupID`


-- Create new table `tbUserGroup`.
-- `tbUserGroup` : Table of tbUserGroup
-- 	`GroupID` : GroupID identifies tbUserGroup
-- 	`GroupName` : GroupName is of tbUserGroup
-- 	`GroupDesc` : GroupDesc is of tbUserGroup
-- 	`isActive` : isActive is of tbUserGroup
-- 	`isDeleted` : isDeleted is of tbUserGroup
-- 	`DeletedDate` : DeletedDate is of tbUserGroup  
create table `tbUserGroup` ( 
	`GroupID` TEXT(18),
	`GroupName` TEXT(20),
	`GroupDesc` TEXT(255),
	`isActive` CHAR(1),
	`isDeleted` CHAR(1),
	`DeletedDate` TEXT(10), constraint `tbUserGroup_PK` primary key (`GroupID`) ); 

-- Note: Access does not support the mandatory (not null) constraint in SQL DDL.
-- The mandatory constraints for table '`tbUserGroup`' will be created in the database using DAO
-- only if there is a connection to the Access MDB file during DDL generation.
-- Not nullable columns for table 'tbUserGroup' :
-- 	`GroupID`


-- Add foreign key constraints to table `tbAccessRole`.
alter table `tbAccessRole`
	add constraint `tbUserGroup_tbAccessRole_FK1` foreign key (
		`GroupID`)
	 references `tbUserGroup` (
		`GroupID`); 

alter table `tbAccessRole`
	add constraint `tbFormList_tbAccessRole_FK1` foreign key (
		`FormName`)
	 references `tbFormList` (
		`FormName`); 

-- Add foreign key constraints to table `tbUserInGroup`.
alter table `tbUserInGroup`
	add constraint `tbUserGroup_tbUserInGroup_FK1` foreign key (
		`GroupID`)
	 references `tbUserGroup` (
		`GroupID`); 

alter table `tbUserInGroup`
	add constraint `tbUser_tbUserInGroup_FK1` foreign key (
		`UserID`)
	 references `tbUser` (
		`UserID`); 


-- This is the end of the Microsoft Visual Studio generated SQL DDL script.
